#!/bin/sh
# Generated by /home/top/workspace/openjdk7/hotspot/make/linux/makefiles/buildtree.make
. ./env.sh
if [ "" != "" ]; then { echo Cross compiling for ARCH , skipping gamma run.; exit 0; }; fi
if [ -z $JAVA_HOME ]; then { echo JAVA_HOME must be set to run this test.; exit 0; }; fi
if ! ${JAVA_HOME}/bin/java -d64 -fullversion 2>&1 > /dev/null
then
  echo JAVA_HOME must point to 64bit JDK.; exit 0;
fi
rm -f Queens.class
${JAVA_HOME}/bin/javac -d . /home/top/workspace/openjdk7/hotspot/make/test/Queens.java
[ -f gamma_g ] && { gamma=gamma_g; }
./${gamma:-gamma} -Xbatch -showversion Queens < /dev/null
